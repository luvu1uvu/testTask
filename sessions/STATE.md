# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-15, сессия 4

## Готово

- Спецификация продукта «Трекер рисков проекта» (`SPEC.md`) — согласована (сессия 1).
- Часть II `AGENTS.md` заполнена и согласована с пользователем (сессия 2):
  описание проекта, стек и версии (чистые HTML5/CSS3/JS, ES-модули, без сборки
  и без npm, `localStorage`, запуск через `python3 -m http.server 8765`),
  команды установки/запуска/тестов/линтера, категоричные запреты, случаи
  обязательной остановки и вопроса, чек-лист перед финальным ответом,
  соглашения проекта (именование, язык кода/комментариев/UI, структура
  модулей, стиль коммитов). Часть I `AGENTS.md` не менялась.
- В ходе согласования пользователь дал существенно более детальные ответы,
  чем предложенные варианты — они перенесены в `AGENTS.md` дословно, а не
  сокращены.
- Обнаружены и переспрошены два внутренних противоречия в одном из ответов
  пользователя (язык идентификаторов в коде; ES-модули vs `<script>` без
  модулей) — оба разрешены явным уточняющим вопросом, детали в
  `sessions/session-2.md`.
- Git-репозиторий инициализирован, создан минимальный `.gitignore`
  (`.DS_Store`, `*.swp`, `*~`), сделан исходный коммит
  `3404617b659696117ccd77b3bafb55233175f607` — `docs: согласовать
  спецификацию и правила` (19 файлов: `.gitignore`, `AGENTS.md`, `CLAUDE.md`,
  `README.md`, `SPEC.md`, `homework.pdf`, `materials/*`, `sessions/*`).
  Git-identity локальная (не глобальная), настроена пользователем самим
  (сессия 3).
- Наивный прогон выполнен (сессия 4): в отдельной, физически изолированной
  папке `/Users/ranis/Documents/AI/TestTask-naive` (вне основного проекта,
  без `AGENTS.md`/`CLAUDE.md`/`SPEC.md`) в чистой сессии Claude Code одним
  коротким промптом без уточняющих вопросов реализован «Трекер рисков
  проекта» на чистых HTML/CSS/JS с `localStorage`. Результат без изменений
  скопирован в основной репозиторий, в `naive-run/` (`risk-tracker/index.html`,
  `risk-tracker/app.js`, `risk-tracker/styles.css`, `.claude/launch.json`);
  идентичность копии подтверждена `diff -rq` и сравнением SHA-256 всех
  файлов. Исходная папка `TestTask-naive` не изменялась и впредь не должна
  изменяться — результат наивного прогона зафиксирован как есть и
  исправлению не подлежит. Подробности, дословный промпт и факты о ходе
  наивного прогона — в `sessions/session-4.md`.

## В работе

Ничего не в работе — сессия 4 завершена на этапе фиксации наивного прогона.

## Не начато

- Реализация продукта по спецификации (`SPEC.md` + Часть II `AGENTS.md`).
- Проверка результата (запуск через `python3 -m http.server`, `tests.html`,
  основной сценарий и один нештатный, полный чек-лист критериев приёмки).
- Независимый аудит другой моделью или чистой сессией.
- Сравнение наивного прогона (`naive-run/`) с итоговой реализацией по спеке.
- `README.md` с итоговым отчётом и сравнением прогонов (текущий `README.md`
  зафиксирован в исходном коммите как есть, не проверялся и не менялся в
  сессиях 3–4).

## Активные инструменты

Кроме Claude Code и Git, ничего не задействовано. Для реализации потребуется
только `python3` (уже подтверждён в окружении: 3.9.6, `/usr/bin/python3`) —
дополнительных библиотек, CLI-утилит или сервисов стек не предполагает
(сознательное решение, зафиксировано в `AGENTS.md`). Отдельно от основного
проекта, вне репозитория, существует папка `TestTask-naive` — исходная
среда наивного прогона; в основной проект и его инструменты не входит,
хранится отдельно и больше не изменяется.

## Известные проблемы

- Раздел «Открытые вопросы» в `SPEC.md` формально не содержит нерешённых
  пунктов — оба вопроса, попавшие туда, были закрыты пользователем в том же
  обмене репликами (известно с сессии 1, не переоценивалось в сессии 2).
- В этом песочничном окружении отсутствует Node.js (`node` не найден) — это
  подтверждено и учтено в выборе стека и способа тестирования, но стоит
  перепроверить `python3`/браузер на машине, где будет проходить финальная
  приёмка, если она проходит в другом окружении.
- Известная неточность в `sessions/session-2.md`: раздел «Изменения в
  проекте» не упоминает `sessions/STATE.md`, хотя он был обновлён в конце
  той же сессии. Файл `session-2.md` закрыт и по правилам курса задним
  числом не переписывается — расхождение зафиксировано в `session-3.md`.
- В ходе сессии 3 агент один раз нарушил прямое указание пользователя и сам
  выполнил `git config --local user.name/email` с фиктивными значениями
  вместо того, чтобы попросить пользователя сделать это в своём терминале.
  Ошибка замечена и откачена (`--unset`) в течение той же сессии, до
  создания коммита — на итоговый результат не повлияла. Подробности и
  формулировка инцидента — в `sessions/session-3.md`, раздел «Размышления».

## Следующий шаг

Реализация продукта по согласованной спецификации (`SPEC.md`) и настройкам
Части II `AGENTS.md`, с проверками по чек-листу из `AGENTS.md` перед каждым
«готово». После реализации — сравнение с зафиксированным наивным прогоном
(`naive-run/`) для итогового отчёта в `README.md`.
